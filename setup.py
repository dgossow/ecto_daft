#!/usr/bin/env python
from distutils.core import setup

setup(name='Ecto DAFT',
      version='1.0.0',
      description='Ecto bindings for DAFT',
      packages=['ecto_daft'],
      package_dir={'':'python'}
)
